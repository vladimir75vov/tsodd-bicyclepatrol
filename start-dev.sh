#!/bin/bash

# Цвета для вывода
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
CYAN='\033[0;36m'
NC='\033[0m' # No Color

echo ""
echo "═══════════════════════════════════════════════════════════"
echo "           tsodd-bicyclepatrol Development Server"
echo "═══════════════════════════════════════════════════════════"
echo ""

# Переход в директорию frontend
cd "$(dirname "$0")/frontend" || exit 1

# Проверка Node.js
echo "[1/3] Проверка Node.js..."
if ! command -v node &> /dev/null; then
    echo -e "${RED}❌ Node.js не установлен!${NC}"
    echo "   Скачайте с https://nodejs.org/"
    exit 1
fi

NODE_VERSION=$(node -v)
echo -e "${GREEN}✅ Node.js ${NODE_VERSION}${NC}"

# Проверка зависимостей
echo ""
echo "[2/3] Проверка зависимостей..."
if [ ! -d "node_modules" ]; then
    echo -e "${YELLOW}⚠️  node_modules не найден. Установка зависимостей...${NC}"
    npm install
    if [ $? -ne 0 ]; then
        echo -e "${RED}❌ Ошибка установки зависимостей${NC}"
        exit 1
    fi
else
    echo -e "${GREEN}✅ Зависимости установлены${NC}"
fi

# Запуск сервера
echo ""
echo "[3/3] Запуск сервера разработки..."
echo ""
echo -e "${CYAN}🚀 Сервер будет доступен по адресу:${NC}"
echo "   http://localhost:3000"
echo ""
echo -e "${CYAN}💡 Для остановки нажмите Ctrl+C${NC}"
echo ""

npm run dev
