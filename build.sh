#!/bin/bash

# Цвета для вывода
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
CYAN='\033[0;36m'
NC='\033[0m' # No Color

echo ""
echo "═══════════════════════════════════════════════════════════"
echo "           tsodd-bicyclepatrol Production Build"
echo "═══════════════════════════════════════════════════════════"
echo ""

# Переход в директорию frontend
cd "$(dirname "$0")/frontend" || exit 1

# Проверка Node.js
echo "[1/4] Проверка Node.js..."
if ! command -v node &> /dev/null; then
    echo -e "${RED}❌ Node.js не установлен!${NC}"
    exit 1
fi

NODE_VERSION=$(node -v)
echo -e "${GREEN}✅ Node.js ${NODE_VERSION}${NC}"

# Установка зависимостей
echo ""
echo "[2/4] Установка зависимостей..."
npm ci --silent
if [ $? -ne 0 ]; then
    echo -e "${RED}❌ Ошибка установки зависимостей${NC}"
    exit 1
fi
echo -e "${GREEN}✅ Зависимости установлены${NC}"

# Линтинг
echo ""
echo "[3/4] Проверка кода (ESLint)..."
npm run lint
if [ $? -ne 0 ]; then
    echo -e "${YELLOW}⚠️  Есть предупреждения ESLint${NC}"
fi

# Сборка
echo ""
echo "[4/4] Сборка проекта..."
npm run build
if [ $? -ne 0 ]; then
    echo -e "${RED}❌ Ошибка сборки${NC}"
    exit 1
fi

# Статистика
echo ""
echo "═══════════════════════════════════════════════════════════"
echo -e "${GREEN}✅ Сборка успешно завершена!${NC}"
echo "═══════════════════════════════════════════════════════════"
echo ""
echo "📁 Результат: frontend/out/"
echo ""
echo "📊 Статистика:"
HTML_COUNT=$(find out -name "*.html" 2>/dev/null | wc -l)
echo "   - HTML страниц: $HTML_COUNT"
SIZE_MB=$(du -sh out 2>/dev/null | cut -f1)
echo "   - Размер: $SIZE_MB"
echo ""
